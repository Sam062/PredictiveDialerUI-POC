server:
  port: 9695
spring:
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/dialer
    username: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
        hbm2ddl:
          auto: update
        show_sql: false
